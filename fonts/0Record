#1. Generate a file of wanted icons using
	https://icomoon.io/
	https://icomoon.io/app/#/select

#2. For our site we have selected:
#set IcoMoon - Free
	home2	(e901)
	github	(eab0)

#3. Generate fonts and download:
	icomoon.zip
	unzip icomoon.zip

#4. Convert fonts to base64:
	cd fonts
	base64 -i -w 0 icomoon.woff > icomoon.base64

#5. Edit ../_sass/icomoon/_base.scss and replace INSERT-BASE64-FONTS-HERE by the content of icomoon.base64

@font-face {
        font-family: 'icomoon';
        src: url(data:application/x-font-ttf;charset=utf-8;base64,INSERT-BASE64-FONTS-HERE) format('truetype');
        font-weight: normal;
        font-style: normal;
}


adding jetstream:
view-source:https://use.jetstream-cloud.org/assets/theme/images/favicon.ico
... save as jetstream.ico
gimp jetstream.ico
... save as PNM file: jetstream.pnm
potrace-1.13.linux-x86_64/potrace jetstream.pnm -s --flat -o jetstream.svg
# from http://potrace.sourceforge.net/
go back to icomoon.io/app/#/select
import icons:	icomoon.svg (current set)
import icons:	jetstream.svg
select all three and generate fonts; as before,with addtion jetstream (e900)
edit _sass/icomoon/_base.scss and add entry for jetstream
